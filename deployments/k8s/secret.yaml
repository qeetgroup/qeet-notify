# Template — DO NOT commit real values. Populate via sealed-secrets, Vault, or CI env injection.
apiVersion: v1
kind: Secret
metadata:
  name: qeet-notify-secrets
  namespace: qeet-notify
type: Opaque
stringData:
  DATABASE_URL: "postgres://user:password@postgres:5432/qeet_notify?sslmode=require"
  ENCRYPTION_KEY: "REPLACE_ME"
  AWS_SES_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SES_SECRET_ACCESS_KEY: "REPLACE_ME"
  RESEND_API_KEY: "REPLACE_ME"
  MSG91_API_KEY: "REPLACE_ME"
  TWOFACTOR_API_KEY: "REPLACE_ME"
  META_WHATSAPP_TOKEN: "REPLACE_ME"
  META_WHATSAPP_PHONE_ID: "REPLACE_ME"
  META_WHATSAPP_VERIFY_TOKEN: "REPLACE_ME"
