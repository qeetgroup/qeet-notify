apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: qeet-notify-api
  namespace: qeet-notify
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: qeet-notify-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
